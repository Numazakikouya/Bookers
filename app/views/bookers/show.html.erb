<!-- あとで上の文を機能として実装する -->
 <% if flash[:notice] %>
      <div class="flash">
        <%= flash[:notice] %>
      </div>
    <% end %>

<h3>Title:　<%= @book.title %></h3>


<h3>Body:　<%= @book.body %></h3>

<%=link_to "Edit",edit_book_path(@book.id) %>
|
<%=link_to "Back",back_path %>
